{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"preview\" ]; then pnpm db:migrate; fi && pnpm build",
  "crons": [
    {
      "path": "/api/demo/reset",
      "schedule": "5 7 * * *"
    }
  ]
}
